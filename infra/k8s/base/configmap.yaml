apiVersion: v1
kind: ConfigMap
metadata:
  name: workspace-config
data:
  ENVIRONMENT: production
  LOG_LEVEL: INFO
  PUBLIC_APP_URL: https://workspace.example.com
  API_BASE_URL: https://workspace.example.com
  NEXT_PUBLIC_API_URL: https://workspace.example.com/api/v1
  NEXT_PUBLIC_WS_URL: wss://workspace.example.com/ws
  S3_BUCKET: ai-workspace-artifacts
