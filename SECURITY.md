# Security Policy

Report vulnerabilities privately. Include reproduction steps, affected components, expected impact, and any logs that do not contain secrets.

Do not commit real OAuth client secrets, JWT secrets, provider API keys, private keys, or production database URLs. Use `.env.example` for names only and a secret manager for production values.
